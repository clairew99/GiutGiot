name: GIUTGIOT
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  ## 20240731 SHJ
    ## stt,tts
  speech_to_text: ^6.1.1
  flutter_tts: ^3.6.3
  permission_handler: ^10.2.0
    ## postural analysis
  sensors: ^2.0.0
  shared_preferences: ^2.0.6
  tflite_flutter: ^0.10.1
  syncfusion_flutter_sliders: ^19.2.55

  connectivity_plus: ^1.0.6
  socket_io_client: ^2.0.0-beta.5
  #  screen: ^0.0.5
  ## SHJ FIN

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true


  ## 20240802 SHJ
  ## 자세 모델

  # 24.08.03 JJY
  # 백그라운드 배경 화면 적용 완료
  # 모든 파일에 접근할 수 있도록 활성화 (예정)
  assets:
    - assets/model/posture_analysis.tflite
    - assets/background/bg_hourglass.gif
    - assets/background/bg.gif
    - assets/background/hourglass.png
  ## SHJ FIN
